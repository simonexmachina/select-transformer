{
  "name": "select-transformer",
  "version": "1.0.2",
  "description": "Transforms HTML select elements into groups of radio buttons or checkboxes",
  "authors": [
    "Simon Wade"
  ],
  "keywords": [
    "dom"
  ],
  "license": "MIT",
  "homepage": "https://github.com/aexmachina/select-transformer",
  "repository": {
    "type": "git",
    "url": "http://github.com/aexmachina/select-transformer.git"
  },
  "main": "dist/cjs/main.js",
  "scripts": {
    "test": "npm run build && bower install && testem ci --launch PhantomJS",
    "build": "rm -rf dist && broccoli build dist",
    "prepublish": "npm run build"
  },
  "files": [
    "assets",
    "dist",
    "lib"
  ],
  "devDependencies": {
    "bower": "^1.2.8",
    "broccoli": "^0.13.2",
    "broccoli-cli": "0.0.1",
    "broccoli-dist-es6-module": "^0.2.1",
    "broccoli-merge-trees": "^0.2.1",
    "broccoli-static-compiler": "^0.2.1",
    "testem": "^0.6.18"
  }
}
